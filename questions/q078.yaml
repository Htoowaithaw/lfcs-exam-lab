id: q078
title: "Build staged source package lfcs-hello-op2"
domain: "Operations & Deployment"
topic: "compile from source"
difficulty: medium
distro: ubuntu
vms: [node1]
question: |
  Build the staged source tarball /opt/lfcs-src/lfcs-hello-1.0.tar.gz.
  Install the program as /usr/local/bin/lfcs-hello-op2 and make it print LFCS-COMPILED-2.
hints: ["Extract the tarball under /usr/local/src.", "Adjust the program's printed message in the Makefile, then build and install the binary."]

id: q091
title: "Build and run local Docker image lfcs-op-docker2"
domain: "Operations & Deployment"
topic: "docker"
difficulty: medium
distro: ubuntu
vms: [node1]
question: |
  Using only the preloaded image lfcs-local-base:1.0, build image lfcs/lfcs-op-docker2:1.0 from /root/lfcs-op-docker2/Dockerfile.
  Run a detached container named lfcs-op-docker2-ctr with label lfcs.question=q091.
hints: ["Do not pull from a remote registry.", "Use docker inspect to verify labels."]

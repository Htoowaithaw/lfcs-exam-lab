id: q092
title: "Build and run local Docker image lfcs-op-docker3"
domain: "Operations & Deployment"
topic: "docker"
difficulty: medium
distro: ubuntu
vms: [node1]
question: |
  Using only the preloaded image lfcs-local-base:1.0, build image lfcs/lfcs-op-docker3:1.0 from /root/lfcs-op-docker3/Dockerfile.
  Run a detached container named lfcs-op-docker3-ctr with label lfcs.question=q092.
hints: ["Do not pull from a remote registry.", "Use docker inspect to verify labels."]

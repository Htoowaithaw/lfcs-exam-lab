id: q154
title: "Persist mount /mnt/lfcs-fstab4"
domain: "Storage"
topic: "fstab boot mounts"
difficulty: medium
distro: ubuntu
vms: [node1]
question: |
  Create an ext4 filesystem labeled fstab4 on /dev/sde1, mount it at /mnt/lfcs-fstab4, add a UUID-based /etc/fstab entry, and ensure mount -a succeeds.
hints: ["Use UUID rather than a raw device path.", "Validation runs mount -a."]
